# 给 Codex 的主任务提示词

请打开本仓库并完成以下任务：

1. 阅读 `AGENTS.md`、`PRD.md`、`README.md`，检查现有 Streamlit MVP。
2. 运行 `pytest -q` 和 `streamlit run app.py`，修复所有错误。
3. 保持“无演示数据、全部由用户手动录入”的产品要求。
4. 完善以下功能：
   - 基于 `stage_history` 计算真实阶段转化率、平均阶段停留天数和阶段流失率；
   - 客户详情页，集中展示基础资料、跟进时间线、阶段历史和订单；
   - 可编辑的客户标签，例如“钢厂”“铸造厂”“大客户”“待送样”；
   - 逾期跟进、七天内应跟进和长期未联系三类提醒；
   - 城市、客户类型、销售负责人维度的筛选与仪表盘；
   - 数据导入校验和重复企业识别；
   - GitHub 写入冲突的重试或合并策略；
   - 将 `app.py` 拆分为清晰的 pages/components，避免单文件继续膨胀。
5. 不得把 GitHub Token 写入任何受版本控制的文件。
6. 为新增逻辑编写 pytest 测试，并更新 README。
7. 最后输出：修改摘要、文件清单、测试结果、部署步骤和仍存在的风险。

验收标准：
- 空数据时界面正常。
- 能新增客户、记录跟进、转换阶段、保存订单并导出。
- 每次阶段变化都会写入 `stage_history`。
- GitHub 模式下数据写入指定分支。
- 所有测试通过。
